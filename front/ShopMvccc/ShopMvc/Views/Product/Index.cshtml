@model IEnumerable<ShopMvc.Models.Product>
@{
    ViewData["Title"] = "Sản phẩm";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold mb-0">Danh sách sản phẩm</h2>
    <!-- Chỉ cần giữ class btn-primary và xóa phần style chỉnh màu tím đi -->
    <a asp-action="Add" class="btn btn-primary">+ Thêm sản phẩm</a>
</div>

<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var product in Model)
    {
        <div class="col">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(product.ImageUrl))
                    {
    <img src="@product.ImageUrl" class="card-img-top"
         style="height:160px;object-fit:cover;" alt="@product.Name" />
}
                    <h5 class="card-title">@product.Name</h5>
                    <p class="card-text small text-muted">@product.Description</p>
                    <p class="fw-bold text-primary">@product.Price.ToString("#,##0") ₫</p>
                    <div class="d-flex gap-2">
                        <a asp-action="Display" asp-route-id="@product.Id" class="btn btn-sm btn-outline-primary">Xem</a>
                        <a asp-action="Update" asp-route-id="@product.Id" class="btn btn-sm btn-outline-secondary">Sửa</a>
                        <a asp-action="Delete" asp-route-id="@product.Id" class="btn btn-sm btn-outline-danger">Xóa</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
@section Scripts {
    <script>console.log("script riêng của trang này");</script>}